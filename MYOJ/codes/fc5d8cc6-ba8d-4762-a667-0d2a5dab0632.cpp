#include <iostream>

int main() {
    int a, b;

    // Read two integers from input
    std::cin >> a >> b;

    // Print the sum of the two integers
    std::cout << (a + b) << std::endl;

    return 0;
}
