#include <stdio.h>

int main() {
    int a, b;

    // Read two integers from input
    scanf("%d %d", &a, &b);

    // Print the sum of the two integers
    printf("%d\n", a - b);

    return 0;
}
